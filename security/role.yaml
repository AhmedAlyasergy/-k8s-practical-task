apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: taskhub-pod-reader
  namespace: taskhub
  labels:
    app: taskhub
rules:
  - apiGroups: [""]
    resources: ["pods"]
    verbs: ["get", "list", "watch"]
