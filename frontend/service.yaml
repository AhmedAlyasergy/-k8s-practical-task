apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: taskhub
  labels:
    app: taskhub
    component: frontend
spec:
  type: ClusterIP
  selector:
    app: taskhub
    component: frontend
  ports:
    - port: 80
      targetPort: 80
