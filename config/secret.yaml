# NOTE: These are LAB-ONLY placeholder values.
# Never commit real passwords, tokens, or credentials to Git.
apiVersion: v1
kind: Secret
metadata:
  name: taskhub-secret
  namespace: taskhub
  labels:
    app: taskhub
    environment: lab
type: Opaque
stringData:
  DATABASE_USERNAME: "taskhub_user"
  DATABASE_PASSWORD: "lab-only-password-123"
  API_TOKEN: "lab-only-token-abc123"
